{{#each blogPosts}}
<article>
  <h2>{{this.title}}</h2>
  <p>Posted by {{this.User.username}} on {{formatDate this.createdAt}}</p>
  <p>{{truncateText this.content 100}}</p>
  <a href="/blogs/{{this.id}}">Read More</a>
</article>
{{/each}}
